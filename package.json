{
  "name": "hse-dashboard",
  "version": "1.0.0",
  "description": "HSE (Health, Safety, Environment) Dashboard - Node.js demo with full CRUD, JSON file persistence. Designed so the API layer can later be swapped for a Laravel backend.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "multer": "^2.0.0"
  }
}
